# Embedded Runtime

Place the embedded MLX runtime here for app-distributed inference.

Required executable path inside bundle resources:

- `EmbeddedRuntime/venv/bin/mlx_lm.server`

The XPC service launches this executable directly.
