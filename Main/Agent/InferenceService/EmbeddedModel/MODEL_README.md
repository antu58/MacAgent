# Embedded Model

Place the bundled model files here.

Required directory inside bundle resources:

- `EmbeddedModel/Qwen3.5-0.8B-8bit/`

This directory should contain a complete local model package usable by:

- `mlx_lm.server --model <local-path>`
