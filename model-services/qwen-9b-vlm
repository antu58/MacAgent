#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
# shellcheck source=/dev/null
source "$SCRIPT_DIR/_model_service.sh"

model_service_main "qwen-9b-vlm" "Qwen3.5 9B VLM local service" \
  "Qwen_9_B" "http://127.0.0.1:18083/v1" "$@"
